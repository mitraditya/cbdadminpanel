import * as React from "react";
import { Card, CardContent, CardHeader } from '@material-ui/core';

const Dashboard = () => (
    <Card>
        <CardHeader title="Welcome to the admin panel!" />
        <CardContent>Cashback Duniya</CardContent>
    </Card>
);

export default Dashboard;